{
  "name": "wiliot-demo-mqtt-nodejs",
  "version": "1.0.0",
  "dependencies": {
    "mqtt": "^5.3.5",
    "dotenv": "^2.0.0"
  },
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "node index.js"
    }
}
